// Caption: Cards are a convenient means of displaying content composed of different types of objects. They’re also well-suited for presenting similar objects whose size or supported actions can vary considerably, like photos with captions of variable length.
mixin card(it)
  .row
    .col.s12.m5
      if it.title || it.links
        .card(class="#{it.bgColor} #{it.size}")
          if it.image
            if it.reveal
              .card-image.waves-effect.waves-block.waves-light
                img.activator(src="#{it.image}")
            else
              .card-image
                img(src="#{it.image}")
                span.card-title.white-text= it.title

          .card-content(class="#{it.descriptionColor}-text")
            if it.reveal
              span.card-title.activator.grey-text.text-darken-4= it.title
                i.mdi-navigation-more-vert.right
              each link in it.links
                p
                  a(href='##{link.href}')= link.text

            if !it.image
              - it.titleColor = it.titleColor || 'grey-text text-darken-4'
              span.card-title(class="#{it.titleColor}-text")= it.title

            if !it.reveal
              p= it.description

          if it.links && !it.reveal
            .card-action
              each link in it.links
                a(href='##{link.href}')= link.text

          if it.reveal
            .card-reveal
              span.card-title.grey-text.text-darken-4= it.title
                i.mdi-navigation-close.right
              p= it.description

      else
        .card-panel(class="#{it.bgColor}")
          span(class="#{it.descriptionColor}-text") !{it.description}


if patternLibrary
  // Title: Card panel
  // hideScript: true
  // Description: For a simple card all you need to do is pass in is the <code>description</code> property. <code>bgColor</code> and <code>descriptionColor</code> add extra styling customization. Feel free to use any value for this found in <a href="#colors">Colors</a>.
  +card({
    bgColor: "teal",
    descriptionColor: "white",
    description: "I am a very simple card. I am good at containing small bits of information. I am convenient because I require little markup to use effectively. I am similar to what is called a panel in other frameworks."
  })

  // Title: Basic Card
  // Description: If you add a <code>description</code> property or an array of <code>links</code> you will automatically be upgraded to the basic card.
  // hideScript: true
  +card({
    bgColor: "blue-grey darken-1",
    descriptionColor: "white",
    titleColor: "white",
    title: "Card Title",
    description: "I am a very simple card. I am good at containing small bits of information. I am convenient because I require little markup to use effectively.",
    links: [
      {
        href: "#",
        text: "This is a link"
      },
      {
        href: "#",
        text: "This is a link"
      }
    ]
  })

  // Title: Image Card
  // Description: In order to add an image to your card it is a simple as that. Just add the <code>image</code> property.
  // hideScript: true
  +card({
    title: "Card Title",
    titleColor: "white",
    image: "http://dummyimage.com/600x400/7190d6/fff.png&text=J",
    description: "I am a very simple card. I am good at containing small bits of information. I am convenient because I require little markup to use effectively.",
    links: [
      {
        href: "#",
        text: "This is a link"
      },
      {
        href: "#",
        text: "This is a link"
      }
    ]
  })

  // Title: Reveal Card
  // Description: Here you can add a card that reveals more information once clicked. In order to do this all you need is <code>reveal: true</code>.
  +card({
    reveal: true,
    title: "Card Title",
    titleColor: "white",
    image: "http://dummyimage.com/600x400/7190d6/fff.png&text=J",
    description: "I am a very simple card. I am good at containing small bits of information. I am convenient because I require little markup to use effectively.",
    links: [
      {
        href: "#",
        text: "This is a link"
      }
    ]
  })

  // Title: Card Sizes
  // hideScript: true
  // Description: In order to change the height of that card define the <code>size</code> property. Available Values: <code>small</code> <code>medium</code> <code>large</code>
  +card({
    size: "large",
    title: "Card Title",
    titleColor: "white",
    image: "http://dummyimage.com/600x400/7190d6/fff.png&text=J",
    description: "I am a very simple card. I am good at containing small bits of information. I am convenient because I require little markup to use effectively.",
    links: [
      {
        href: "#",
        text: "This is a link"
      },
      {
        href: "#",
        text: "This is a link"
      }
    ]
  })





